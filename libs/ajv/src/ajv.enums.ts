/**
 * Enum representing the custom format keys for the ajv validation pipe.
 */
export enum AjvFormatKey {
  PHONE_NUMBER = 'phone-number',
}

/**
 * Enum representing the custom keyword keys for the ajv validation pipe.
 */
export enum AjvKeywordKey {}
